// import { useSelector } from "react-redux";

const Loader = () => {
    // const isLoading = useSelector((state) => state.status);

    return <div className={`loader  ? "" : "inactive"}`}></div>;
};

export default Loader;
